{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "build:pwa": "vite build --mode production",
        "build:analyze": "vite build --mode analyze",
        "preview": "vite preview",
        "clean": "rm -rf public/build public/hot",
        "rebuild": "npm run clean && npm run build",
        "pwa:test": "npm run build && php artisan serve --host=0.0.0.0 --port=8000",
        "pwa:optimize": "node scripts/pwa-build.js",
        "pwa:build": "npm run build && npm run pwa:optimize"
    },
    "devDependencies": {
        "@tailwindcss/forms": "^0.5.7",
        "@tailwindcss/typography": "^0.5.10",
        "autoprefixer": "^10.4.16",
        "axios": "^1.6.2",
        "laravel-vite-plugin": "^1.0.0",
        "postcss": "^8.4.32",
        "tailwindcss": "^3.3.6",
        "terser": "^5.44.0",
        "vite": "^5.0.8"
    },
    "dependencies": {
        "@fortawesome/fontawesome-free": "^7.1.0",
        "alpinejs": "^3.13.3"
    }
}
